/** 
 * @brief quotient of two ints 
 * @param aNum the dividend 
 * @param bNum the divisor 
 * @return quotient of a divided by b 
 */ 
#include <stdio.h>
#include <math.h>
extern int story;
extern double time;

int quote(int aNum, int bNum); 

double timeToFallFeet(int story);